Template repo for a new package (system or module) for FVTT.

How to use:
- clone
- rename relevant files
- add a `module.json` or `system.json` file
- optional: remove `package-lock.json` and run `npm i`
- run `npm ci`
